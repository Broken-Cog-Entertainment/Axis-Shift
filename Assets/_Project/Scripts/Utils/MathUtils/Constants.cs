using UnityEngine;

namespace AS.Utils.MathUtils
{
    public static class Constants
    {
        public const float PI = Mathf.PI;
        public const float TAU = PI * 2;
        public const float PI_BY_2 = PI * 0.5f;
    }
}